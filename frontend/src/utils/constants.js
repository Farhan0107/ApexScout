/**
 * ApexScout Metric Limits
 * These must remain in sync with the backend normalization engine.
 */
export const METRIC_LIMITS = {
    speed: 100,
    verticalLeap: 60,
    wingspan: 120,
    pointsPerGame: 150,
    assists: 50,
    stamina: 100
};

export const CHART_SCALE_MAX = 100; // Using 0-100 for backend consistency
